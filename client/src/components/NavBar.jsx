import React from 'react';
import './NavBar.css'; 

const NavBar = () => {
    return (
        <nav>
            <a href="#">Blog</a>
            <a href="#">Search</a>
            <a href="#">Merch</a>
            <a href="#">Extra</a>
        </nav>
    );
}

export default NavBar;